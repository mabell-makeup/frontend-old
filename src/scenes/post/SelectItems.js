import React from "react"
import {SafeAreaView} from "react-native"
import {Text} from "react-native-paper"

const createStyles = () => ({
})


export const SelectItems = ({navigation}) => {

  return (
    <SafeAreaView>
      <Text>SelectItems</Text>
    </SafeAreaView>
  )
}